import React, { FunctionComponent } from "react";
import Main from "./main_page_tasks";

type Props = {
    
}

const Mai: FunctionComponent<Props> = (props: Props) => {
    return (
        <Main/>
    );
};

export default Mai;